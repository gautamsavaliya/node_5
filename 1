#include<stdio.h>
#define n 10
int a[n],pos=0,i,j;
void lastinsert(int b)
{
    if(pos>n)
    {
        printf("array is full");
    }
    else
    {
        a[pos++]=b;
    }
}
void print()
{
    for(i=0;i<pos;i++)
    {
        printf("\n%d",a[i]);
    }
}
int main()
{
    int b;
    lastinsert(1);
    lastinsert(2);
    lastinsert(3);
    lastinsert(4);
    lastinsert(5);
    lastinsert(6);
    lastinsert(7);
    lastinsert(8);
    lastinsert(9);
    lastinsert(10);
}